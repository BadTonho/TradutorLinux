[package]
name = "tl_rust_validator"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/rust/validator.rs"
crate-type = ["staticlib"]

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
